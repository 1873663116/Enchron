# 组件契约:`LoadingSpinner`(加载转圈)

> 状态:已定稿(契约层)。未实现。**保留**(未来加载态必需)。

## 0. 角色
不确定进度的加载指示(转动的弧)。

## 1. 构造
动画弧(`arcStart`/`arcEnd` 旋转),可选外环边框。动画时长走 `DesignTokens.LoadingSpinner`。

## 2. 变体
无。

## 3. 暴露参数
| 参数 | 能调什么 | 影响 | 证据 |
|---|---|---|---|
| `size` | 转圈尺寸 | 整体大小;默认 56 | 橱窗演示多尺寸 |
| `showBorder` | 是否显示外环 | 有/无边框 | 可变 |

## 4. 无障碍(硬要求)
- 暴露「加载中」状态给辅助技术。

## 5. 锁死
动画时长(`DesignTokens.LoadingSpinner`)、配色、线宽比例——全锁。

## 6. 决策记录
1. **保留**:虽 `live=0`、仅橱窗,但它是以后必要的加载动画(远程文件加载等)。`DesignTokens.LoadingSpinner` token 随之保留,不进死 token 清单。
