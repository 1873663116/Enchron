---
{"actor":{"actorId":"agent:operability:e3554585bebe070c","environmentDigest":"sha256:8af0ca905197f3c0b3f730d84f3bfd2f7645e54c04d1c3dee18a3ba015a2e5a3"},"issuedAt":"2026-08-29T08:40:00Z","packetDigest":"sha256:e3554585bebe070c944adc893caaa127a2a35cc5ca101ced2b5e1040c7ee01b5","packetId":"review-packet:agent-operability-e3554585bebe070c","reviewer":"agent-operability","schema":"enchron.regression.review-report","schemaVersion":1,"usage":{"inputTokens":11023,"reviewItems":9}}
---

# Agent operability review

This report accepts `review-packet:agent-operability-e3554585bebe070c` (`sha256:e3554585bebe070c944adc893caaa127a2a35cc5ca101ced2b5e1040c7ee01b5`).

Every packet unit has an accepted decision and a non-empty rationale.

## Canonical assessment

    {"actor":{"actorId":"agent:operability:e3554585bebe070c","environmentDigest":"sha256:8af0ca905197f3c0b3f730d84f3bfd2f7645e54c04d1c3dee18a3ba015a2e5a3"},"issuedAt":"2026-08-29T08:40:00Z","packetDigest":"sha256:e3554585bebe070c944adc893caaa127a2a35cc5ca101ced2b5e1040c7ee01b5","reviewer":"agent-operability","schema":"enchron.regression.agent-operability-assessment","schemaVersion":1,"units":[{"contentDigest":"sha256:9126350c454e6ccf1d104cc427fa576f60b68f2ac6052fa68f17bc8a8e2a1d86","decision":"accepted","kind":"journey","rationale":"Accepted. The Journey groups 8 Scenario(s) on lane requirement(s) ['both', 'device', 'either'] and declares shared state [StateRequirement(key='local-aggregate-staged', schema='fixture-set.local-aggregate-staged@2')]. Its 4 ordering edge(s) (injected-import-rejoins-ingest -> artwork-captured-on-exit; injected-import-rejoins-ingest -> audio-track-switch-same-session; injected-import-rejoins-ingest -> external-subtitle-source-matrix; injected-import-rejoins-ingest -> subtitle-switch-and-off) each express a real shared-state dependency: the predecessor establishes library or source state the successor consumes, both ends belong to this Journey, and their lane requirements intersect so the edge stays lane-local. No edge names the MainGate Scenario, so no MainGate dependency becomes a Journey join. Every scenario reference resolves to a Catalog Scenario in this Journey.","ref":"journey:local-media-lifecycle"},{"contentDigest":"sha256:f42135ce053554d4ff5995e7448eca9756bcd17019ec6a16e82385cf04daf734","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 6 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 06 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.artwork-captured-on-exit.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 300000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:artwork-captured-on-exit"},{"contentDigest":"sha256:d33867fa65451797fb99c700253cec18446d0e12aad4c716163bac6e8a0ca8f2","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 12 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). All 1 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 3 static case(s) ['unique-label', 'duplicate-label-index-0', 'duplicate-label-index-1'] is represented by concrete ordered calls and carries exactly one obligation: unique-label <- 08 (operation:evidence.capture-audio@2) as audio.measurement/audio-measurement@2 judged by oracle:agent-audio@2 under rubric:local-media-lifecycle.audio-track-switch-same-session.o01@1; duplicate-label-index-0 <- 10 (operation:evidence.capture-audio@2) as audio.measurement/audio-measurement@2 judged by oracle:agent-audio@2 under rubric:local-media-lifecycle.audio-track-switch-same-session.o01@1; duplicate-label-index-1 <- 12 (operation:evidence.capture-audio@2) as audio.measurement/audio-measurement@2 judged by oracle:agent-audio@2 under rubric:local-media-lifecycle.audio-track-switch-same-session.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 130000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:audio-track-switch-same-session"},{"contentDigest":"sha256:95e199efb8338b7e8f961625b262051d64f7a5dcab7a17135324ce8b1c5bd0b7","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 26 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). All 1 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 26 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:local-media-lifecycle.automatic-play-next-resume-policy.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 435000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:automatic-play-next-resume-policy"},{"contentDigest":"sha256:781383b3acbf4aa50862fc046f9718b2ed65bf00e16c9772c3461d8ebfeeae2f","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 7 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on both bound lanes including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 06 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:local-media-lifecycle.clean-flat-playback-main-gate.o01@1; default <- 07 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.clean-flat-playback-main-gate.o02@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 122000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution. It is the MainGate for ['BoundLane.DEVICE', 'BoundLane.SIMULATOR']; the requirement is lane both, which yields exactly one gate attempt per lane, and no Journey ordering edge touches it, so the gate dependency stays lane-local and never becomes a Journey join.","ref":"scenario:local-media-lifecycle:clean-flat-playback-main-gate"},{"contentDigest":"sha256:3993d4e105c6f873bd2d18c850ce4a8ef48ecc58b44133f1c3d164bc37706250","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 6 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on either bound lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 06 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:local-media-lifecycle.clean-start-position-zero.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 120000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:clean-start-position-zero"},{"contentDigest":"sha256:d72195d378428679528caf6116c78fd8e4e8234ae1f3ab9cb9bf606efb935d7b","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 21 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2, local-directory-subtitle-source-ready/media-source.local-directory-sidecars@1, webdav-test-source-ready/remote-source.webdav-fixture@2, emby-test-library-ready/remote-source.emby-library@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator), preparation:local-directory-subtitle-source (device), preparation:webdav-test-source (device), preparation:emby-test-library (device). Each of the 3 static case(s) ['local-sidecar', 'webdav-sidecar', 'emby-external-stream'] is represented by concrete ordered calls and carries exactly one obligation: local-sidecar <- 06 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.external-subtitle-source-matrix.o01@1; webdav-sidecar <- 14 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.external-subtitle-source-matrix.o01@1; emby-external-stream <- 21 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.external-subtitle-source-matrix.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 420000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:external-subtitle-source-matrix"},{"contentDigest":"sha256:843b017fa79cc8168a963dc75067437d699df1bf0f04e5dba6b1700147365f24","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 4 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on either bound lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 01 (operation:media.import-staged@2) as library.command/library-command@1 judged by oracle:agent-structured-library-command@1 under rubric:local-media-lifecycle.injected-import-rejoins-ingest.o01@1; default <- 04 (operation:accessibility.inspect@2) as accessibility.tree/accessibility-tree@1 judged by oracle:agent-structured-accessibility-tree@1 under rubric:local-media-lifecycle.injected-import-rejoins-ingest.o02@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 30000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:injected-import-rejoins-ingest"},{"contentDigest":"sha256:74624bcb9cd13a95e9d2c2492e3bf8bf3dbe2f528c525ec8ddac6aa4af6ff5cd","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 13 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite local-aggregate-staged/fixture-set.local-aggregate-staged@2 is established without a human runtime actor by preparation:local-aggregate-device (device), preparation:local-aggregate-simulator (simulator). Each of the 4 static case(s) ['embedded-text', 'embedded-bitmap', 'off', 'restore'] is represented by concrete ordered calls and carries exactly one obligation: embedded-text <- 07 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.subtitle-switch-and-off.o01@1; embedded-bitmap <- 09 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.subtitle-switch-and-off.o01@1; off <- 11 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.subtitle-switch-and-off.o01@1; restore <- 13 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:local-media-lifecycle.subtitle-switch-and-off.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 128000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:local-media-lifecycle:subtitle-switch-and-off"}],"usage":{"inputTokens":11023,"reviewItems":9}}
