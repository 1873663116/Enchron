---
{"actor":{"actorId":"agent:operability:6f47073ae1d286b3","environmentDigest":"sha256:36a409769f89bc2cfc6476a6ef4c0cc60f71019e18ec32d082701810ec1d7399"},"issuedAt":"2026-08-29T08:40:00Z","packetDigest":"sha256:6f47073ae1d286b3d7c5a6f19dc4434f66b432e3910919070a69a680b4882bd5","packetId":"review-packet:agent-operability-6f47073ae1d286b3","reviewer":"agent-operability","schema":"enchron.regression.review-report","schemaVersion":1,"usage":{"inputTokens":11055,"reviewItems":7}}
---

# Agent operability review

This report accepts `review-packet:agent-operability-6f47073ae1d286b3` (`sha256:6f47073ae1d286b3d7c5a6f19dc4434f66b432e3910919070a69a680b4882bd5`).

Every packet unit has an accepted decision and a non-empty rationale.

## Canonical assessment

    {"actor":{"actorId":"agent:operability:6f47073ae1d286b3","environmentDigest":"sha256:36a409769f89bc2cfc6476a6ef4c0cc60f71019e18ec32d082701810ec1d7399"},"issuedAt":"2026-08-29T08:40:00Z","packetDigest":"sha256:6f47073ae1d286b3d7c5a6f19dc4434f66b432e3910919070a69a680b4882bd5","reviewer":"agent-operability","schema":"enchron.regression.agent-operability-assessment","schemaVersion":1,"units":[{"contentDigest":"sha256:8b734781ee253a2512010a4d80271ec86d0d3b497fa3721626b3a6226d40171d","decision":"accepted","kind":"journey","rationale":"Accepted. The Journey groups 6 Scenario(s) on lane requirement(s) ['device'] and declares shared state [StateRequirement(key='faultable-remote-source-ready', schema='remote-source.faultable@2')]. It declares no ordering edge, so its Scenarios carry no artificial sequencing and may be scheduled independently. No edge names the MainGate Scenario, so no MainGate dependency becomes a Journey join. Every scenario reference resolves to a Catalog Scenario in this Journey.","ref":"journey:network-resilience"},{"contentDigest":"sha256:310e9f95c8cc4b2c717e2f305e00905bf98f25e2d6cabde0f6ff05cfdcff31c4","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 20 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite faultable-remote-source-ready/remote-source.faultable@2 is established without a human runtime actor by preparation:faultable-remote-source (device). All 6 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 2 static case(s) ['paired-control', 'buffer-absorbed-interruption'] is represented by concrete ordered calls and carries exactly one obligation: paired-control <- 10 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:network-resilience.buffered-reconnect-has-no-indicator.o01@1; buffer-absorbed-interruption <- 20 (operation:evidence.capture-frames@1) as visual.frames/frame-sequence@2 judged by oracle:agent-visual@2 under rubric:network-resilience.buffered-reconnect-has-no-indicator.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 400000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution. This Scenario was re-checked specifically: each of the two attempts now runs a preceding operation:diagnostics.playback-state@1 call with expectation webdav-loopback (:09 and :19), and the two operation:evidence.capture-frames@1 calls (:10 and :20) bind productBindingDigest to result://.../:09/bindingDigest and result://.../:19/bindingDigest respectively. That handler does return a top-level bindingDigest when an expectation is set (Scripts/verification/regression_operation_adapter.py:5431-5447), so both visual.frames producers are reachable and both o01 obligations can be judged.","ref":"scenario:network-resilience:buffered-reconnect-has-no-indicator"},{"contentDigest":"sha256:cf120689f7c0d12cf75e8e4f99f337e149e6e6dddb6dd556bbf037ef9ebf0e0e","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 10 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite faultable-remote-source-ready/remote-source.faultable@2 is established without a human runtime actor by preparation:faultable-remote-source (device). All 3 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 10 (operation:diagnostics.surface-probe@1) as interaction.trace/interaction-trace@1 judged by oracle:agent-structured-interaction-trace@1 under rubric:network-resilience.certificate-change-stops-without-trust.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 390000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:network-resilience:certificate-change-stops-without-trust"},{"contentDigest":"sha256:7035faee84c553534102b19fc007852fc4813f11fec979c3b912afc1a2550517","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 12 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite faultable-remote-source-ready/remote-source.faultable@2 is established without a human runtime actor by preparation:faultable-remote-source (device). All 8 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 12 (operation:diagnostics.surface-probe@1) as interaction.trace/interaction-trace@1 judged by oracle:agent-structured-interaction-trace@1 under rubric:network-resilience.finite-backoff-reconnect.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 390000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:network-resilience:finite-backoff-reconnect"},{"contentDigest":"sha256:5fe4c9daa0c5debc6534c1c1347697332d6f2b06ba421b23b99cbb466890e743","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 52 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite faultable-remote-source-ready/remote-source.faultable@2 is established without a human runtime actor by preparation:faultable-remote-source (device). All 4 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 4 static case(s) ['connection-interrupted', 'file-missing', 'access-refused', 'data-corrupt'] is represented by concrete ordered calls and carries exactly one obligation: connection-interrupted <- 13 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:network-resilience.playback-failure-category-matrix.o01@1; file-missing <- 26 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:network-resilience.playback-failure-category-matrix.o01@1; access-refused <- 39 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:network-resilience.playback-failure-category-matrix.o01@1; data-corrupt <- 52 (operation:diagnostics.playback-state@1) as playback.probe/playback-probe@1 judged by oracle:agent-structured-playback-probe@1 under rubric:network-resilience.playback-failure-category-matrix.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 610000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:network-resilience:playback-failure-category-matrix"},{"contentDigest":"sha256:57fe0ad3683df0d45486998c9b13860ea88b8e8df4cfa2214d423f5636d228eb","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 1 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite faultable-remote-source-ready/remote-source.faultable@2 is established without a human runtime actor by preparation:faultable-remote-source (device). Each of the 1 static case(s) ['default'] is represented by concrete ordered calls and carries exactly one obligation: default <- 01 (operation:evidence.structural-test@1) as structural.test/structural-test@2 judged by oracle:agent-structured-structural-test@2 under rubric:network-resilience.prefetch-without-waiting-consumer.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 70000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:network-resilience:prefetch-without-waiting-consumer"},{"contentDigest":"sha256:1ba4ad824ce81a1564f2b1c0345f9bf0363323d03a48deb498f17718ca161941","decision":"accepted","kind":"scenario","rationale":"Accepted. One Sidekick can finish this Scenario in one lease and one lane attempt: all 12 calls are ordered in front matter, carry unique call IDs and invocation limits, and every one passes OperationSpec.validate on the Device lane including the adapter's closed choice sets, numeric bounds and cross-validators, so each has concrete arguments, a supported lane and a real current execution route. Prerequisite faultable-remote-source-ready/remote-source.faultable@2 is established without a human runtime actor by preparation:faultable-remote-source (device). All 8 result:// arguments name an earlier call in this attempt and select a top-level output field that the producing handler actually returns. Each of the 1 static case(s) ['single-read-failure'] is represented by concrete ordered calls and carries exactly one obligation: single-read-failure <- 12 (operation:diagnostics.surface-probe@1) as interaction.trace/interaction-trace@1 judged by oracle:agent-structured-interaction-trace@1 under rubric:network-resilience.recoverable-read-resumes-from-checkpoint.o01@1. Each obligation's evidence pair is among its producer Operation's declared outputs and is accepted by its Oracle, and the SuccessExpression is a conjunction over exactly those obligations, so the archive holds the observations needed to judge every obligation and no case can be dropped. The declared cost 390000 ms is a plausible upper bound for these cases and bounded retries given the per-call deadlines. readiness is ready with no blockers and no HumanCoverage question blocks execution.","ref":"scenario:network-resilience:recoverable-read-resumes-from-checkpoint"}],"usage":{"inputTokens":11055,"reviewItems":7}}
