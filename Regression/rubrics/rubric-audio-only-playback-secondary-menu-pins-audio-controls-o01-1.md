---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:audio-only-playback.secondary-menu-pins-audio-controls.o01@1",
  "title": "Secondary Menu Pins Audio Controls",
  "criteria": [
    "relatedResults[0] is the More-menu activation's tappedIdentifiers: exactly the ordered list registered for the bound case, audio-menu=[PlayerUI-window-playback-surface, PlayerUI-TopAction-more, PlayerUI-menu-audio] and speed-menu=[PlayerUI-window-playback-surface, PlayerUI-TopAction-more, PlayerUI-menu-speed]. relatedResults[1] is that same call's tapSequence response, succeeded, reporting one command that tapped three elements in sequence, so the nested route was delivered without a second controller round trip.",
    "The bound probe's own interactionTrace, measured from the pre-route cursor this Scenario passes as cursorToken, carries both halves of the menu presentation in order: reachability top actions delivered action=menu.more, recorded when the More popover's content appeared, and reachability top secondary menu visible=true menu=more, recorded by ProductionPlaybackMoreMenu on the same appearance. The second line is the one that reaches setControlsFocused on an audio-only asset, because PlayerInfoBarView.swift:41 removes the PlaybackTopActions row that otherwise sends it, and no reachability top secondary menu visible=false menu=more follows it anywhere in this trace, so the popover was still presented when the reading below was taken.",
    "The bound control-plane fields, read after the 9000 ms settle that outlasts the 8 s controls auto-hide window this Scenario's Preparation pins, report mediaKind audioOnly, lifecycle Playing and controls=shown. mediaKind is the discriminator that the branch under test was the branch taken: it is the value PlayerInfoBarView.swift:41 and WindowPlayerDeck.swift:417 both switch on, so a video reading would mean the absence claims below were made against a top action row that was never removed. controls=shown is what the open menu bought -- canAutoHideControls is controlsAutoHideSeconds > 0 and showControls and isControlsFocused == false (PlaybackSessionModel.swift:629-631), and with the settle longer than the window the reading can only stand if something set isControlsFocused.",
    "relatedResults[2] is the same activation's assertAbsentObservations, read inside the runner against the whole application rather than the single main-window tree a snapshot prints, so it can decide a claim about rows that live in a system popover. In the observations recorded after step PlayerUI-TopAction-more, with the More menu open, PlayerUI-TopAction-videoFormat, PlayerUI-TopAction-dock and PlayerUI-TopAction-resumePanorama each report exists false, because PlayerInfoBarView.swift:41 drops the whole PlaybackTopActions group for an audio-only asset; PlayerUI-menu-subtitles reports isEnabled false, which is the claim this product makes -- the Subtitles row is always rendered, since subtitleItems always carries its Off entry, and audio-only disables it rather than removing it, so isEnabled and not exists is the field that separates offered from withheld. That the popover was readable at all is established by the same call: its third step tapped PlayerUI-menu-audio (speed-menu: PlayerUI-menu-speed), a row inside that popover, through the same application-wide query, so a null reading is not what produced these four values."
  ],
  "negativeControls": [
    "A relatedResults[0] identifier list that differs from the bound case, an omitted PlayerUI-TopAction-more step, a PlayerPanel-menu-* route on a window host, or a split nested-menu route — more than one activation bound to the case, or a response that does not report the whole route in one sequence — fails the bound case.",
    "A controller-only success with no reachability top actions delivered action=menu.more line in the bound interactionTrace, a trace with no reachability top secondary menu visible=true menu=more or one where a visible=false menu=more follows it, mediaKind video, controls=hidden at the end of the 9000 ms settle, an enabled PlayerUI-menu-subtitles, or an existing PlayerUI-TopAction-videoFormat, -dock or -resumePanorama in the bound assertAbsentObservations fails the bound case."
  ]
}
---
# Secondary Menu Pins Audio Controls

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
