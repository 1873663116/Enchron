---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:presentation-tour.track-selection-survives-seek-and-presentation.o01@1",
  "title": "Track Selection Survives Seek And Presentation",
  "criteria": [
    "The bound attempt opens registered WebDAV sdr-bframe-aggregate-30s.mkv and relatedResults record two complete resident window menu transactions: PlayerUI-window-playback-surface→PlayerUI-TopAction-more→PlayerUI-menu-audio→PlayerUI-menu-audio-2, then surface→TopAction-more→PlayerUI-menu-subtitles with label sdr-bframe-aggregate-30s.zh-CN.srt.",
    "The bound post-mutation state preserves the same media identity and selected audio/subtitle track identities after the mutation registered for its case: seek, docked round trip, portal/window format round trip, or one format replacement. A bound pure spatial transition case also preserves the media session identity."
  ],
  "negativeControls": [
    "The nonexistent Commentary or external-reference identifiers, a same-label/different-ID fallback, split nested-menu controllers, selection loss, or session replacement during the docked pure transition fails the rubric."
  ]
}
---
# Track Selection Survives Seek And Presentation

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
