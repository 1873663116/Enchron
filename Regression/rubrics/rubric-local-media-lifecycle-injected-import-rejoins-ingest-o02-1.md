---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:local-media-lifecycle.injected-import-rejoins-ingest.o02@1",
  "title": "Injected Import Persists In Product Grid",
  "criteria": [
    "After relaunch, the accessibility tree exposes exactly one media card with the imported file name."
  ],
  "negativeControls": [
    "A missing card, duplicate card, different file name, or a card from a stale pre-relaunch tree violates the persistent-grid contract."
  ]
}
---
# Injected Import Persists In Product Grid

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
