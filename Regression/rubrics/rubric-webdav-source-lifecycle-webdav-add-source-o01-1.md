---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:webdav-source-lifecycle.webdav-add-source.o01@1",
  "title": "Webdav Add Source",
  "criteria": [
    "One resident accessibility transaction activates FileBrowsing-SourcesSidebar-sourceMore, FileBrowsing-SourcesSidebar-add, and FileBrowsing-SourcesSidebar-addWebDAV in order before any field is typed.",
    "The successful transaction activates connect and certificate Trust, then dismisses the future-warning action labelled 以后; the bound post-state exposes the registered WebDAV source and sdr-bframe-aggregate-30s.mkv card."
  ],
  "negativeControls": [
    "Opening addWebDAV directly, splitting the nested source-menu route across controllers, or leaving the 以后 prompt unresolved is not the declared user route.",
    "A controller-only success, a source created by direct state injection, or a password value echoed in evidence cannot satisfy the rubric."
  ]
}
---
# Webdav Add Source

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
