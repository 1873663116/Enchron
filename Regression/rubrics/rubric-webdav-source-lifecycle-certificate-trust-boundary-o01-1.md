---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:webdav-source-lifecycle.certificate-trust-boundary.o01@1",
  "title": "Certificate Trust Boundary",
  "criteria": [
    "The first compact connect→Cancel transaction closes the certificate alert while retaining the same open WebDAV form: name, address, and username remain unchanged, and the password field is empty in the bound cancelled-form snapshot.",
    "Without reopening the source route or retyping non-secret fields, only the password is restored; connect→Trust followed by 以后 creates the source, opens the registered aggregate fixture, and reaches Playing.",
    "The final interaction trace binds the baseline cursor, cancelled-form product binding, host generation, certificate fingerprint, and product delivery in one ordered sequence; no certificate trust prompt occurs during playback reconnection."
  ],
  "negativeControls": [
    "Reopening addWebDAV, retyping name/address/username after Cancel, retaining or exposing the password, or accepting Trust before the second connect violates the draft-state boundary.",
    "Overlapping form and certificate alert, reversed delivery sequence, missing fingerprint, unresolved 以后 prompt, or a controller-only success cannot satisfy the rubric."
  ]
}
---
# Certificate Trust Boundary

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
