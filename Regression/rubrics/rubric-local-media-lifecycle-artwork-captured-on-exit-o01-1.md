---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:local-media-lifecycle.artwork-captured-on-exit.o01@1",
  "title": "Artwork Captured On Exit",
  "criteria": [
    "The bound capture inlines three frame manifests and three artwork readings of one attempt, and an exact digest chain decides the claim, which is the comparison HC-021 asks for before any measured bound. relatedResults carries the readings as three consecutive six-value groups in the order evidence.capture-frames@1 emits them -- artworkKey, artworkCurrentDigest, artworkStoredDigest, artworkStoredBytes, artworkByteStreamScope, artworkByteStreamRequestCount -- indices 0-5 the pre-exit reading, indices 6-11 the reading taken after the first exit, indices 12-17 the reading taken after the reopen and before the second exit. In the pre-exit reading artworkCurrentDigest (index 1) is a real SHA-256 for the frame the session was displaying while artworkStoredDigest (index 2) is none and artworkStoredBytes (index 3) is zero, because the attempt opened by clearing the artwork cache. The reading taken after the first exit is on the same artworkKey (index 6 equals index 0) and has a real SHA-256 artworkStoredDigest (index 8) with artworkStoredBytes (index 9) above zero: the exit wrote artwork where there was none. That exit is PlayerUI-InfoBar-button-back, which reaches saveCurrentArtwork through stopPlayback and cancelPlaybackLaunchAndPersistProgress (Modules/Playback/PlaybackLaunchCoordinator.swift:551-589). Its four frames are the second inlined manifest, taken on the browser after the exit, so the fixture's card carries that artwork; set against the pre-exit playback frames of the first manifest it shows the same scene rather than placeholder art, a solid fill, or a different moment of the clip. No numeric crop or colour-distance tolerance is applied and none may be invented: HC-021 still lists measure-artwork-bound as required work, and the digest chain above is the exact comparison it says to prefer.",
    "A second exit at a different position replaces the first artwork. The attempt reopens the fixture and waits past a position later than the one the first exit left, and the third inlined reading, taken there on the same artworkKey (index 12 equals index 0), still reports the first exit's artworkStoredDigest -- index 14 equals index 8, because nothing but an exit rewrites the store -- while its artworkCurrentDigest (index 13) differs from the pre-exit reading's (index 1), so the frame about to be stored is a different one. The bound capture, taken after that second exit through the same key, reports in its own output an artworkStoredDigest that differs from index 8 with artworkStoredBytes above zero. Its own artworkKey equals index 0, index 6 and index 12, so one media identity is in play throughout."
  ],
  "negativeControls": [
    "A 1x1, corrupt, single-frame, or provenance-free capture is Indeterminate and never Satisfied, as is an artworkStoredDigest of none in a reading taken after an exit.",
    "Pure black, a solid-colour card, or a post-exit card showing a different scene from the pre-exit capture's frames is a negative control that forces Violated once capture validity is established. Repetition inside one manifest is not one of them here: calls 06 and 07 tap PlayerPanel-button-play and await lifecycle paused before call 08 captures, calls 14 and 15 do the same before call 16, and calls 09 and 17 leave the browser standing before calls 10 and 18, so four consecutive equal screenshotDigest values inside any one of these manifests are the state this Scenario establishes and decide nothing either way. What must differ across manifests is the artwork digest chain the criteria read, not the frames inside a manifest.",
    "A stale prior frame, placeholder art, missing card art, a second exit that leaves the first exit's artworkStoredDigest in place, or an artworkKey that changes between readings violates the rubric."
  ]
}
---
# Artwork Captured On Exit

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
