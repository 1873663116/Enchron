---
{
  "schema": "enchron.regression.rubric",
  "schemaVersion": 1,
  "id": "rubric:presentation-tour.format-editor-hosts-window-and-portal.o01@1",
  "title": "Format Editor Hosts Window And Portal",
  "criteria": [
    "The bound case delivers the editor transaction registered for its host, read from the producer's own return and its own relatedResults. Window inlines relatedResults[0..1], the surface→PlayerUI-TopAction-videoFormat→PlayerUI-VideoFormat-cancel transaction that opened and dismissed the editor once, and reads the reopened editor from its own return. Portal inlines relatedResults[0], the Playback surface label activation, relatedResults[1..2], the compact videoFormat→cancel sequence, and relatedResults[3], the Playback surface label activation that immediately precedes the bound activation. Each inlined response succeeded and names its own tapped identifiers or label. Every activation in either case whose route taps PlayerUI-window-playback-surface or its Playback surface label declares dismissControls true, so each such step raises the chrome: the surface routes to a bare showControls.toggle() (PlaybackSessionModel.swift:579-589), the presentation Preparations leave the auto-hide window at the runner's 300 s, and an activation that inherits a shown chrome would hide the deck it is meant to raise.",
    "Each host case binds the editor to one activation's own return and never to a later controller reading. PlayerUI-VideoFormat is the content of a .popover anchored on PlayerUI-TopAction-videoFormat (Modules/Playback/Views/PlaybackTopActions.swift:603-607 and :806) that dismissMenu tears down the moment chrome hides (:551-553), and references/product.md:21 records that such a panel is outside the printed main-window tree: snapshot --identifier takes no matchedElement from it and reachability is the tap's own return. The window case is therefore one succeeded tapSequence over PlayerUI-window-playback-surface then PlayerUI-TopAction-videoFormat whose alsoInspected array reports PlayerUI-VideoFormat, PlayerUI-VideoFormat-automatic, PlayerUI-VideoFormat-apply and PlayerUI-VideoFormat-cancel as exists=false after the surface step and as existing, enabled and hittable after the videoFormat step, so the editor and its registered controls were read inside the one command that opened them and appeared only because of the videoFormat step. The portal case binds its own successful videoFormat activation together with the inlined Playback surface activation responses, including the matched public elements that live outside the main-window hierarchy and are therefore evidenced by each activation's own return rather than by hierarchy text. The window producer's exists=false reading is a consequence of that declaration and not of the preceding call: dismissControls lowers the chrome through the idempotent toggleControls command (references/product.md:35, TestCommandChannel.swift:1070-1074) before the route runs, so the surface step raises the chrome, the editor is closed at that moment, and the four rows can only appear at the videoFormat step."
  ],
  "negativeControls": [
    "Addressing a window-only surface identifier from portal, splitting one transient sheet action across controllers, reading the editor from a snapshot issued after the transaction that opened it, an alsoInspected record whose editor rows already exist after the surface step, inspecting only element existence, reading a transaction that the producer does not inline through relatedResults, or using a diagnostic injection route cannot satisfy either host case."
  ]
}
---
# Format Editor Hosts Window And Portal

The Oracle evaluates only the bound case artifact and returns a structured result for every criterion and negative control.
