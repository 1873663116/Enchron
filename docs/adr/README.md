# ADR — 架构决策记录

ADR 只保存已经发生的重大取舍，不是当前规格。当前产品能力见 `../product-requirements.md`，当前模块边界见仓库根 `ARCHITECTURE.md`。

只有以下三条全部满足时才新增 ADR：

1. 难以逆转——之后改主意的代价是实质性的；
2. 缺上下文会让未来读者困惑"为什么这样做"；
3. 真实权衡的产物——存在过认真的替代方案。

格式：标题即决策、状态、日期、背景、决策、考虑过的替代方案、后果。当前行为永远以产品规格、架构边界和生产代码为准。
