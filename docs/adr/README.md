# ADR — 架构决策记录

ADR 只保存已经发生的重大取舍，不是当前规格，也不证明记录中的实现仍然存在。当前产品结果和实现从生产代码、构建配置和运行结果查证，仓库根 `ARCHITECTURE.md` 只提供导航。

只有以下三条全部满足时才新增 ADR：

1. 难以逆转——之后改主意的代价是实质性的；
2. 缺上下文会让未来读者困惑"为什么这样做"；
3. 真实权衡的产物——存在过认真的替代方案。

格式：标题即决策、状态、日期、背景、决策、考虑过的替代方案、后果。实际实现以当前代码和运行证据为准。
