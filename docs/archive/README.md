# Enchron 文档归档

本目录保存已经不再代表当前产品、架构或验证规则的历史材料，包括被取代的 ADR。当前 Agent 不得从归档文件推导有效产品行为、实现方式、测试入口或发布门槛。

当前状态从生产代码进入，导航见 `ARCHITECTURE.md`；在办的工作见 `docs/plans/`；证据边界见 `.agents/skills/vp-e2e/features/` 与 `Scripts/verification/run_verification.py`。本目录里出现的已删除文档，去向登记在 `Config/retired_documents.json`。

归档材料只用于追溯当时的调查、证据和决策背景。文件中出现的命令、工具限制、测试环境和术语均保持其历史含义。
