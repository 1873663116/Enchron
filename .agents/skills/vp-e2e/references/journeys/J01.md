# J01 汇总片源：导入、播放、音轨与字幕连续切换

Generated by `Scripts/verification/regression_journeys.py reference`. Do not hand-edit.

- 触发：`standing`
- 覆盖：`media-import`, `clean-state-playback`, `track-selection`, `cache-and-artwork`

## 内容条件

内容条件：sdr-bframe-aggregate-30s.mkv 汇总片源——视频流与既有 SDR 基准片逐字节一致；四条跨编码音轨各带独特脉冲频率（AAC 880Hz、FLAC 660Hz、AC-3 440Hz、E-AC-3 550Hz），刻意跨越解码与透传两条管线；三条内嵌字幕轨互异可辨（SubRip 中文纯文本、ASS 英文带样式、DVB 英文位图）；两件外挂字幕边车，文本与内嵌轨明显不同以便截图区分内外。生成入口 Scripts/fixtures/generate_acceptance_fixtures.sh，登记于 Tests/Fixtures/fixture-registry.json。

## 中心事实与判据

覆盖：media-import、clean-state-playback、track-selection（选择语义与连续切换两侧：默认规则、菜单构成、关闭与恢复、跨 seek 与跨退出重进的存续，以及六次连续切换）、cache-and-artwork（Artwork 侧）。
内容条件：sdr-bframe-aggregate-30s.mkv 汇总片源——视频流与既有 SDR 基准片逐字节一致；四条跨编码音轨各带独特脉冲频率（AAC 880Hz、FLAC 660Hz、AC-3 440Hz、E-AC-3 550Hz），刻意跨越解码与透传两条管线；三条内嵌字幕轨互异可辨（SubRip 中文纯文本、ASS 英文带样式、DVB 英文位图）；两件外挂字幕边车，文本与内嵌轨明显不同以便截图区分内外。生成入口 Scripts/fixtures/generate_acceptance_fixtures.sh，登记于 Tests/Fixtures/fixture-registry.json。

## 步骤

### 1 · `P0`、`P1`

P0；P1(files 页签)。

### 2 · `P2`

P2(sdr-bframe-aggregate-30s.mkv, "Journey Fixture")。网格出现对应卡片。

### 3 · `P3`、`P8`

P3(该卡片, window)。机械层：lifecycle=Playing、videoVisible=true。判断层：P8(SDR 真实内容)。

### 4 · `P9`

P9(880Hz 主导、非静音)——音画皆在解码，判断层收口。

### 5 · 无基元引用

**默认选择规则**。不做任何选择，读取诊断串报告的活动音频流序号与菜单选中态。产品的规则是三级的：先由解封装库按容器声明的默认位与流质量排出首选；首选流若不受支持则退到第一条受支持的音轨；一条受支持的都没有才以音轨选择错误开不了。判据：音轨菜单中恰有一项处于选中态，该项与诊断串报告的活动流一致，且第 4 步测得的主导频率等于该轨声明的频率。**「选中了某一条」不算通过，必须是规则预言的那一条。**

### 6 · 无基元引用

**默认字幕状态与菜单构成**。产品开播时不自动选择任何内嵌字幕轨，并把「Off」作为固定末项追加在可用字幕轨之后。判据：「Off」处于选中态、其余项均未选中、画面无字幕；菜单项数等于诊断串报告的可用字幕轨数加一，汇总片源上应为三条内嵌轨加两件外挂边车加一项「Off」共六项，各项标题互异可辨。若实测发现某轨被自动选中，本步不判失败，改记现行为并转入待裁决——开播是否应自动选字幕是产品决策，不是缺陷。

### 7 · 无基元引用

轮询诊断串至 position≥10 秒。

### 8 · `P12`

P12(本地)——汇总切换段：音轨三次跨管线切换，字幕三次跨文本／位图与内嵌／外挂切换。

### 9 · 无基元引用

**关闭与恢复**。承接上一步的外挂边车选择，选一条内嵌字幕轨 → 截图确认字幕出现；选「Off」→ 截图确认字幕消失且「Off」为选中态；再选回原轨 → 截图确认字幕重新出现。三步各自双重验证。关闭态与「选了一条空轨」在菜单选中态上必须可区分：关闭是没有选择，不是选了别的。

### 10 · `P9`

**跨 seek 存续**。保持当前音轨与字幕选择，拖动时间轴跳到远处并跨越关键帧边界，稳态后复核两项选择均未变，P9 主导频率仍为所选音轨的频率。格式切换在产品内部就是一次 seek，本步先把 seek 这一层单独钉住，使 J06 与 J07 上出现的选择丢失能被归因到呈现层而非 seek。

### 11 · `P4`

P4(PlayerUI-InfoBar-button-back) 退出播放 → 回到网格，无错误浮层。

### 12 · 无基元引用

采网格卡片区域截图，Agent 判读卡片画面是否为退出前后的画面内容【判断层·此前无人看守，本步建立看守】。

### 13 · 无基元引用

**跨退出重进存续**。再次 tap 同一卡片进入播放，复核音轨与字幕选择。产品按媒体条目持久化轨道偏好，与观看状态、格式偏好、播放模式偏好同处一份记录；字幕偏好把「关闭」与「选定某轨」记为两种不同的取值，因此关闭态本身也会被记住。判据：两项选择与退出前一致；若退出时字幕为关闭，重进后仍应为关闭而非回到默认。

### 14 · `P11`

P11(库中恰一条引用；无 userVisibleIssue)。

## 不证明

- 不证明：Files 选择器与相册两条系统面导入入口（人工层，佩戴者场次各走一遍即终身有效）；画质主观；逐格式解码覆盖（归 J00）；选择跨呈现态转换的存续（归 J07，那里才真正改变呈现）；轨道菜单在退化片源上的形态（归 J00，那里本就逐个打开构成各异的片源）。
