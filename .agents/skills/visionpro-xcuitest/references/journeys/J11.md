# J11 库管理

Generated by `Scripts/verification/regression_journeys.py reference`. Do not hand-edit.

- 触发：`standing`
- 覆盖：`media-import`

## 内容条件

内容条件：至少三件本地样片用于批量操作；注入源目录在主机侧可直接查看。

## 中心事实与判据

覆盖：media-import 的应用内入口、库文件夹的增改删、引用的移动与删除、批量选择、删除的作用域。

**本旅程的中心判据：删除的是引用，不是用户的文件。** 库里存的是对媒体的引用，任何删除动作都不得触及原文件。这一条错了是不可逆的数据损失，因此每个删除步骤都要在主机侧确认原文件仍在——**不是看界面说了什么，是去源目录看文件还在不在**。

内容条件：至少三件本地样片用于批量操作；注入源目录在主机侧可直接查看。

### 步骤

## 步骤

### 1 · `P0`、`P1`、`P2`

P0；P1(files 页签)；P2 注入三件样片。

### 2 · 无基元引用

**新建库文件夹**：管理菜单 → 新建库文件夹 → 命名。判据：文件夹出现在当前层级，计数随之变化。

### 3 · 无基元引用

**改名**：判据：新名生效，其中的引用不受影响。

### 4 · 无基元引用

**移动引用·单个**：移一件引用进该文件夹。判据：源层级计数减一、目标层级计数加一；进入文件夹可见该引用；**该引用仍可正常开播**——移动的是库内组织，不是文件。

### 5 · 无基元引用

**批量选择与移动**：管理菜单 → 多选 → 选中两件 → 移动。判据：选中计数显示正确；移动后两处计数一致；**取消选择后选中状态清空不残留**。

### 6 · 无基元引用

**删除引用·单个**：判据：网格中消失、计数减一、**主机侧确认原文件仍在**。

### 7 · 无基元引用

**批量删除**：选中两件删除。判据同上，逐件确认原文件仍在。

### 8 · 无基元引用

**删除库文件夹**：删除一个内含引用的文件夹。**先记录现行为再定判据**——其中的引用是被一并删除还是移回上一层，两种设计都成立，实测结果写回本条。无论哪种，**主机侧原文件必须仍在**。

### 9 · 无基元引用

**搜索过滤**：在当前层级输入一段查询。产品对文件与文件夹应用同一条实时过滤，按名称不区分大小写包含匹配。判据：匹配项保留、非匹配项消失、**文件与文件夹两类都被过滤**（只过滤其一即缺陷）；条目计数随过滤结果变化；清空查询后全部恢复。**过滤不得改变库本身**——清空查询后引用与文件夹一件不少。

### 10 · 无基元引用

**空状态**：删到当前层级为空。判据：出现专门的空状态元素而不是一片空白；计数显示为零。**与过滤后无结果的空状态是两回事**：前者库里真的没有，后者只是被查询挡住了，两种情形若给出同一个提示即表达缺陷。

### 11 · 无基元引用

**计数一致性**：每一步之后，界面上的条目计数与层级中实际卡片数一致。**计数与实际不符是一类独立缺陷**——它意味着两套状态没有同源，而不是显示错了。

### 12 · `P11`

P11(库状态与逐步操作的预期一致；无残留可见问题)。

## 不证明

- 不证明：系统文件选择器与相册两条导入入口（人工层，佩戴者场次各走一遍）；远程来源的浏览（归各来源旅程）。
