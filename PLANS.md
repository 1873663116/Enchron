# Enchron 执行计划（ExecPlan）

本文档定义执行计划（ExecPlan）的全部要求。ExecPlan 是一份设计文档，coding agent 可以遵循它来交付一个可工作的功能或系统变更。将读者视为本仓库的完全新手：他们只有当前工作树和你提供的这份 ExecPlan 文件。没有先前计划的记忆，没有外部上下文。


## 如何使用 ExecPlan 和 PLANS.md

在编写 ExecPlan 时，必须严格遵循本文件的所有要求。如果本文件不在你的上下文中，先完整阅读 PLANS.md。在生产准确的规格说明时，要彻底阅读（和反复阅读）源代码。创建规格说明时，从骨架开始，边调研边填充。

在执行 ExecPlan 时，不要向用户询问"下一步做什么"；直接推进到下一个里程碑。在每个停顿点保持所有章节的更新，添加或拆分进展条目以明确陈述已完成的工作和后续步骤。自主解决歧义，频繁提交。

在讨论 ExecPlan 时，将所有决策记录到文档的 Decision Log 中供后来者参考；任何对规格说明的变更都必须有清晰的理由。ExecPlan 是活文档，必须始终可以仅从这份 ExecPlan 重新开始工作，不依赖任何其他资料。

在研究具有挑战性需求或重大不确定性的设计时，使用里程碑来实现概念验证、"玩具实现"等，以验证用户的方案是否可行。阅读库的源代码（找到或获取它们），深入研究，并包含原型来指导更完整的实现。


## 什么时候需要 ExecPlan

满足以下任一条件即需要 ExecPlan：

- 新增功能模块或新的限界上下文
- 跨模块重构
- 修改 protocol 接口定义（涉及 Domain/Ports/ 下的文件）
- 涉及 3 个以上文件的关联改动
- 修复已知问题且需要新增/修改架构约束
- 任何预计工作时间超过 2 小时的任务

以下情况不需要 ExecPlan：

- 单文件 bug 修复（但必须更新 REGRESSION.md）
- 纯文档更新
- SwiftLint 规则调整


## 不可协商的要求

- 每份 ExecPlan 必须完整自包含。自包含意味着它在当前形态下包含一个新手成功所需的全部知识和指令。
- 每份 ExecPlan 都是活文档。贡献者必须在取得进展、有新发现、以及设计决策确定时修订它。每次修订后仍必须保持完整自包含。
- 每份 ExecPlan 必须使一个完全的新手能够端到端地实现功能，无需对本仓库的先验知识。
- 每份 ExecPlan 必须产出可示范的工作行为，而不仅仅是"满足定义"的代码变更。
- 每份 ExecPlan 必须用朴素语言定义每一个专业术语，否则不要使用它。

目的和意图放在最前面。先用几句话解释，从用户的角度出发，为什么这项工作很重要：完成这个变更后用户能做什么之前做不到的事，以及如何看到它在工作。然后引导读者完成达到该结果的确切步骤，包括要编辑什么、要运行什么、以及应该观察到什么。

执行你计划的 agent 可以列出文件、读取文件、搜索、运行项目和运行测试。它不知道任何先前的上下文，无法从早期里程碑推断你的意思。重复你依赖的每一个假设。不要指向外部博客或文档；如果需要某个知识，用你自己的话嵌入到计划中。如果一份 ExecPlan 建立在之前的 ExecPlan 之上且该文件已提交到仓库，通过引用路径将其纳入。如果没有提交，你必须包含那份计划中所有相关的上下文。


## 格式要求

当将 ExecPlan 写入一个 Markdown (.md) 文件且文件内容仅包含这份 ExecPlan 时，省略三重反引号包裹。

用朴素散文书写。偏好句子而非列表。避免使用清单、表格和长篇枚举，除非简洁会模糊含义。清单仅在 Progress 章节中允许，且在该章节中是强制性的。叙述性章节必须保持以散文为主。

每个标题后使用两个换行。使用 # 和 ## 等正确的标题语法，以及有序和无序列表的正确格式。


## 写作指南

自包含和朴素语言是最重要的。如果你引入了一个非日常用语的短语（"防腐层"、"聚合根"、"限界上下文"、"帧输出"），立即定义它，并提醒读者它在本仓库中如何体现（例如，命名它出现的文件或目录）。不要说"如前所述"或"根据架构文档"。在这里包含所需的解释，即使你重复自己。

避免常见的失败模式。不要依赖未定义的术语。不要把"功能的字面描述"写得如此狭窄以至于产出的代码能编译但不做任何有意义的事。不要把关键决策外包给读者。当存在歧义时，在计划本身中解决它并解释为什么你选择了那条路径。在过度解释用户可见效果和欠定义附带实现细节之间，偏向前者。

用可观察的结果锚定计划。陈述用户在实现后能做什么、要运行的命令、以及他们应该看到的输出。验收应该表述为人类可以验证的行为（"在真机上打开一个本地 MP4 文件，5 秒内出画出声"）而非内部属性（"添加了一个 MediaProfile 结构体"）。如果变更是内部的，解释如何仍能展示其影响（例如，通过运行之前失败而之后通过的测试，以及展示使用新行为的场景）。

显式指定仓库上下文。使用仓库相对路径命名文件，精确命名函数和模块，描述新文件应该创建在哪里。如果涉及多个区域，包含一段简短的定位段落，解释这些部分如何组合在一起，以便新手可以自信地导航。运行命令时，显示工作目录和确切的命令行。当结果依赖环境时，陈述假设并在合理时提供替代方案。

幂等和安全。写步骤时使其可以多次运行而不造成损坏或偏移。如果一个步骤可能中途失败，包含如何重试或适配。如果需要迁移或破坏性操作，明确写出备份或安全回退方案。偏好可增量验证的增量变更。

验证不是可选的。包含运行测试的指令，如果适用的话启动系统，并观察它做有用的事。为任何新功能或能力描述全面的测试。包含预期输出和错误消息，以便新手能区分成功和失败。在可能的情况下，展示如何证明变更在编译之外是有效的。陈述与项目工具链适配的确切测试命令以及如何解读其结果。

本项目的验证分为两条轨道（详见 TESTING.md）：

- Agent 可自检轨道：swift build、swift test、swiftlint lint、scripts/check-workaround.sh、api/scripts/validate-contract.sh
- 人类真机验证轨道：visionOS 的 UI 流畅度、视觉效果、真机交互必须由人类在真机上验证

每个里程碑的验证章节必须同时覆盖两条轨道。agent 可自检的项直接写命令和预期输出；需要人类真机验证的项写具体操作步骤和预期结果。

捕获证据。当你的步骤产出终端输出、短差异或日志时，将它们作为缩进的示例包含在文档中。保持简洁，聚焦于证明成功的内容。


## 里程碑

里程碑是叙事性的，不是官僚主义。如果你将工作分解为里程碑，用一段简短的段落引入每个里程碑，描述其范围、在里程碑结束时将存在什么之前不存在的东西、要运行的命令、以及你期望观察到的验收结果。保持它像一个故事一样可读：目标、工作、结果、证明。

进展（Progress）和里程碑（Milestones）是不同的：里程碑讲述故事，进展追踪细粒度工作。两者必须都存在。不要仅仅为了简洁而省略里程碑的细节，不要遗漏对未来实现可能至关重要的信息。

每个里程碑必须是可独立验证的，并且增量地实现执行计划的总体目标。


## 原型里程碑和并行实现

当原型里程碑能降低更大变更的风险时，明确鼓励包含它们。示例：在依赖中添加一个低级 API 调用来验证可行性，或者探索两种实现路径并测量效果。保持原型是增量的和可测试的。明确标注范围为"原型"；描述如何运行和观察结果；陈述提升或丢弃原型的标准。

偏好增量的代码变更，然后在保持测试通过的情况下做减法。并行实现（例如在迁移期间保留一个适配器和旧路径）在降低风险或使测试在大型迁移期间继续通过时是可以的。描述如何验证两条路径以及如何安全地用测试退役其中一条。当使用多个新库或功能领域时，考虑创建独立的探针（spike）来评估这些功能的可行性，证明外部库按预期工作并独立实现我们需要的功能。


## 活文档和设计决策

ExecPlan 是活文档。当你做出关键设计决策时，更新计划以记录决策和背后的思考。将所有决策记录在 Decision Log 章节中。

ExecPlan 必须包含并维护 Progress 章节、Surprises & Discoveries 章节、Decision Log 章节、以及 Outcomes & Retrospective 章节。这些不是可选的。

当你发现优化器行为、性能权衡、意外的 bug、或影响你方法的语义时，在 Surprises & Discoveries 章节中捕获这些观察，附上简短的证据片段（测试输出最理想）。

如果你在实现中途改变方向，在 Decision Log 中记录为什么，并在 Progress 中反映其影响。计划既是你的清单，也是下一个贡献者的指南。

在完成主要任务或整个计划后，写一条 Outcomes & Retrospective 条目，总结达成了什么、剩下什么、以及学到的教训。


## ExecPlan 骨架

以下是一份良好 ExecPlan 的骨架。agent 在需要写 ExecPlan 时，从这个骨架开始，边调研边填充：

    # {简短、面向行动的标题}

    本 ExecPlan 是活文档。Progress、Surprises & Discoveries、Decision Log、
    Outcomes & Retrospective 四个章节必须在工作进行中保持更新。

    本文档须遵循 PLANS.md（仓库根目录）的全部要求进行维护。

    ## Purpose / Big Picture

    用几句话解释这个变更完成后用户获得了什么，以及如何看到它在工作。
    陈述你将启用的用户可见行为。

    ## Progress

    使用带复选框的列表总结细粒度步骤。每个停顿点都必须记录在此，
    即使需要将部分完成的任务拆分为两条（"已完成"和"剩余"）。
    本章节必须始终反映工作的实际当前状态。

    - [x] (2026-03-14 13:00) 示例已完成步骤。
    - [ ] 示例未完成步骤。
    - [ ] 示例部分完成步骤（已完成：X；剩余：Y）。

    使用时间戳来衡量进展速率。

    ## Surprises & Discoveries

    记录实现过程中发现的意外行为、bug、优化或洞察。提供简洁的证据。

    - Observation: ...
      Evidence: ...

    ## Decision Log

    记录工作过程中做出的每个决策：

    - Decision: ...
      Rationale: ...
      Date: ...

    ## Outcomes & Retrospective

    在主要里程碑完成或整个计划完成时，总结成果、差距和经验教训。
    将结果与原始 Purpose 对比。

    ## Context and Orientation

    描述与本任务相关的当前状态，假设读者一无所知。
    使用仓库相对路径命名关键文件和模块。定义你将使用的所有非日常术语。
    不要引用先前的计划。

    ## Plan of Work

    用散文描述编辑和新增的顺序。对于每个编辑，命名文件和位置（函数、模块）
    以及要插入或修改什么。保持具体和最小化。

    ## Milestones

    ### M1: {名称}

    （散文）描述范围、完成后存在什么、要运行的命令、预期的验收结果。
    保持像故事一样可读：目标、工作、结果、证明。

    #### 验证（双轨）

    Agent 自检：
    - swift build — 预期无 error
    - swift test — 预期 N 个测试通过
    - swiftlint lint — 预期无 error 级别违规

    人类真机验证：
    - {具体操作步骤} → {预期结果}

    ### M2: {名称}
    ...

    ## Concrete Steps

    陈述要运行的确切命令和运行位置（工作目录）。
    当命令产生输出时，显示简短的预期记录以便读者对比。
    本章节必须随工作进行而更新。

    ## Validation and Acceptance

    描述如何启动或操作系统以及要观察什么。
    将验收表述为行为，包含具体的输入和输出。

    ## Idempotence and Recovery

    如果步骤可以安全重复，说明。
    如果步骤有风险，提供安全的重试或回滚路径。

    ## Artifacts and Notes

    包含最重要的终端记录、差异或代码片段作为缩进示例。
    保持简洁，聚焦于证明成功的内容。

    ## Interfaces and Dependencies

    明确指定。命名要使用的库、模块和服务以及为什么。
    指定在里程碑结束时必须存在的类型、protocol 和函数签名。
    使用稳定的名称和路径，如 PlaybackCore/Domain/Ports/PlaybackControlling.swift。


如果你遵循以上指导，一个单独的、无状态的 agent——或一个人类新手——可以从上到下阅读你的 ExecPlan 并产出一个可工作的、可观察的结果。这就是标准：自包含、自给自足、新手指导、结果导向。

当你修订计划时，必须确保你的变更全面反映在所有章节中（包括活文档章节），并且你必须在计划底部写一条说明，描述变更内容和原因。ExecPlan 不仅要描述做什么，还要描述为什么——几乎对所有事情都是如此。


## 活跃计划索引

| 编号 | 标题 | 状态 | 创建日期 |
|------|------|------|----------|
| （初始为空） |

状态：draft / in-progress / blocked / done

活跃计划存放在 workspace-agents/exec-plans/ 目录下，命名格式：EP-{序号}-{简称}.md


## 归档规则

ExecPlan 标记为 done 后：

1. 完成 Outcomes & Retrospective 章节
2. 移动到 workspace-agents/archive/exec-plans/
3. 从本文件的活跃计划索引中删除
4. 在当月增量总结文档中追加摘要（workspace-agents/archive/summaries/summary-{YYYY-MM}.md）
5. 检查里程碑验收标准是否已转化为 REGRESSION.md 中的回归项


## 历史里程碑

v0.1 技术验证 → v0.2 播放体验完善 → v0.3 远程文件访问 → v0.4 沉浸空间（规划中）

详见 workspace-agents/design_docs/phase4_implementation_roadmap.md
