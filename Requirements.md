# visionOS VR 播放器需求文档

## 1. 产品愿景与目标用户

**愿景**: 一款原生的、高端的空间计算媒体播放器，强调唯美的 3D 环境和自然的空间交互，拒绝简单的 2D 移植

**目标用户**: 追求高质量、沉浸式媒体播放体验，喜欢"开箱即用"的优雅设计而非繁琐折腾的用户；区别于极度硬核的家庭影院发烧友

## 2. 核心功能需求（主干流程与异常处理）

### 2.1 媒体源与格式支持

**文件系统**: 其它文件夹的本地文件、Apple 相册、远程支持 SMB、WebDAV 协议
**格式支持**: 支持主流视频格式（MP4, MKV, AVI, MOV 等）、图片格式（jpeg, png, gif, webp, heif 等），后续计划支持 3D 模型格式（预留后续拓展功能的渠道即可）
**过滤机制**: 系统无法解析或不支持的冷门格式文件将不显示在 UI 中，用户无法选择
**高清格式**: 深度支持各类发烧友级别的高清与原盘格式，包括但不限于 HDR10, HDR10+, 杜比视界, HLG, 以及 4K/8K 超高码率视频解码
**3D 与全景格式**: 支持 3D 立体视频（SBS 左右格式、OU 上下格式）以及全景视频（360° 全景、180° VR、鱼眼投影）。播放器自动识别视频的投影类型并切换对应的渲染模式，同时允许用户手动覆盖
**HDR 自动识别**: 播放器自动检测视频的 HDR 类型（SDR / HDR10 / HDR10+ / 杜比视界 / HLG），并自动配置对应的色彩空间和渲染参数，无需用户手动操作。通过 VideoToolbox 硬件解码保留原始 HDR 元数据，Apple 平台已内置杜比视界解码授权

### 2.2 导航与播放选择

**默认入口**: 应用启动后直接显示 UI 面板
**场景选择**: 面板具备场景选择、设置、文件浏览等功能，用户可以在场景选择界面切换心仪的虚拟场景
**面板缩放**: 用户可以让面板处于最小化状态（只留下下方的 white bar），通过注视该控制按钮并使用双指捏合（点击）来呼出菜单。用户可以设置下次进入软件时默认加载选定的虚拟场景，并将面板置于最小化状态。具体实现方案待研究 visionOS 平台 API 能力后确认，MVP 阶段可降级或推迟
**存储配置**: 系统不会强制引导设置，而是将"添加网络存储"（如 SMB/WebDAV）放置在菜单选项卡最显眼的位置，供用户自行配置
**文件浏览**: 用户浏览映射自数据源的文件夹菜单，支持多种预设的文件排序方式（如按名称、按修改时间、按大小等，均支持升序降序）。后续迭代：添加已导入的本地文件夹路径作为首页显示；再后续：添加类似 Infuse 的封面抓取与媒体库海报墙功能
**选择与播放**: 选定支持的媒体文件后，系统根据当前状态和视频投影类型自动选择播放模式（见 2.3 节三种播放模式）。画面的默认尺寸与空间摆放位置，需参考 Apple visionOS HIG 关于媒体窗口的官方推荐值确定，由开发阶段研究后写入实现

### 2.3 播放模式与空间交互

**三种播放模式**:

- **窗口模式**: 不进入沉浸空间时，视频在普通 SwiftUI 窗口中播放，类似 iPad 体验。适用于平面 2D 视频和 3D SBS/OU 视频
- **沉浸场景模式**: 用户已进入虚拟场景，视频显示在场景中的虚拟屏幕上，通过 Metal 纹理桥接渲染。适用于平面 2D 和 3D SBS/OU 视频。视频开始播放将作为一个"信号"触发 3D 环境光源的改变（MVP 可缺省此特效）。每个虚拟场景具备独立的屏幕位置记忆
- **全景模式**: 播放 360° / 180° / 鱼眼全景视频时自动进入。进入沉浸空间但**不加载虚拟场景**（节省性能），视频投射在包裹用户的球体/半球体内壁上

**播放模式自动切换**: 系统在视频打开时自动识别其投影类型和 HDR 特征（统一为 MediaProfile），并根据当前是否处于沉浸空间来决定播放模式。用户也可通过 UI 手动切换或覆盖

**空间手势（无需 UI 呼出即可完成）**:

**手势消歧机制**: 系统采用延迟判断机制，在首次捏合触发后等待约 200ms 的观察窗口。若在此窗口内触发第二次捏合则识别为「双击」，若捏合持续保持则识别为「长按」，仅当两者均不满足时才识别为「单次捏合」。

**单次捏合**: 唤出主菜单
**快速捏合两次**: 播放/暂停的快捷切换
**捏合并保持**: 激活 2.0 倍速播放，松开后恢复正常速
**按住并向左/向右拖拽**: 直接拉动进度条（模拟移动端操作）
**二级进度条**: 点击进度条后，标准播放器 UI 切换为二级进度条模式，即一个可左右滑动的时间轴。用户可以像视频剪辑软件一样放大或缩小时间刻度——放大=更密集的时间轴，缩小=更稀疏的时间轴。点击视频画面空白区域退出二级进度条模式，同时隐藏所有播放器 UI，完整呈现视频
**选集**: 提供"选集"功能，呈现当前文件夹下的所有视频文件，使用户不必退出当前播放视频，即可选择其他视频播放
**标准控制**: 提供常规的播放、暂停、快进/快退（10 秒落差）、倍速播放（0.25x, 0.50x, 0.75x, 1x, 1.25x, 1.50x, 1.75x, 2.0x, 3.0x, 5.0x）
**音轨与字幕**: 支持多音轨视频和字幕轨道的解析与用户手动选择
**环境视角控制**: 提供一个调节拉条或手柄，允许用户直接沿 X 轴旋转屏幕（以用户为圆心），以适应不同的观影姿势（如躺下观看）
**虚拟屏幕位置调节**: 在沉浸空间播放模式下，播放控制界面提供以下两个调节控件（均记忆上次设置）：
- **远近距离**：调节虚拟屏幕距用户的前后距离（如 2 米 ~ 20 米范围内连续调节）
- **垂直高度**：调节虚拟屏幕的上下位置（适配不同坐姿和个人偏好）

### 2.4 状态管理与异常处理

**网络异常**: 卡顿/缓冲——持续显示转圈动画并尝试继续缓冲播放。断开连接——直接弹出错误提示框，用户点击确认后不退出当前视频，而是停止播放并在后台继续尝试重连（兼顾性能与时效性的轮询或监听机制）
**播放记忆中断**: 记录视频的最后播放进度，用户再次打开未看完的视频时，弹出提示询问"是否继续上次的进度？"弹窗内提供"记住我的选择，下次不再弹窗"的设置选项（并提供全局设置模式）。在文件浏览界面，带有播放进度的视频文件下方会显示上次进度的小字提示。进度识别策略（本地文件按路径识别、远程文件跨 IP 的识别方案）待研究后补充
**缓存清理策略**: 默认清理存储超过 5 天的进度缓存数据。当应用退出时，如果 visionOS 将远程视频流缓存在了闪存中，需执行代码清理逻辑

## 3. 非功能性需求 (NFRs) 与架构约束

**性能策略（不主动降级）**: 播放器严格按照源文件素质进行解码和渲染，系统不提供也不主动执行为挽救掉帧而降低画质的选项。因硬件瓶颈（如 32K 极限码率）导致的性能问题，需由用户自行更换低级别视频源解决
**架构解耦与扩展性（3D 环境光影）**: MVP 阶段可以不实现复杂的"视频屏幕光线影响虚拟空间"的全局光照特效，甚至 MVP 也可以不实现完整的虚拟空间播放模式
**字体规范**: 遵循 visionOS 原生字体系统，不引入自定义字体
**架构级要求**: 设计时必须做到各大逻辑模块的鲁棒和深度解耦（模块内允许耦合，便于 Agent 开发和修复），模块间通过 protocol 接口通信，第三方库通过防腐层隔离。必须预留出必要的接口，确保内部逻辑后续可以方便地添加新功能，而无需大刀阔斧地重构底层

---

## 4. MVP 范围速查表

| 功能模块 | MVP v1.0 | 后续迭代 |
|---|---|---|
| 本地文件播放 | 必须 | — |
| SMB / WebDAV 远程播放 | 必须 | — |
| 主流视频格式（MP4/MKV/AVI/MOV） | 必须 | — |
| HDR 自动识别与渲染（HDR10/杜比视界/HLG） | 必须 | — |
| 文件浏览与多种排序 | 必须 | — |
| 空间手势（含 200ms 捏合消歧机制） | 必须 | — |
| 标准播放控制（进度条/倍速/音轨/字幕） | 必须 | — |
| 二级时间轴进度条（无缩略图） | 必须 | — |
| 选集（当前文件夹视频列表） | 必须 | — |
| 环境视角 X 轴旋转 + 虚拟屏幕远近/高度调节（含记忆） | 必须 | — |
| 播放记忆（进度恢复弹窗） | 必须 | — |
| 网络异常处理与重连 | 必须 | — |
| 窗口模式播放（类 iPad 体验） | 必须 | — |
| 3D SBS/OU 立体视频播放 | 必须 | — |
| 360°/180° 全景视频播放（全景模式） | 必须 | — |
| 视频投影类型自动识别 + 手动覆盖 | 必须 | — |
| 3D 虚拟场景播放模式 | MVP 可缺省 | v2 |
| 视频触发环境光源变化 | MVP 可缺省 | v2 |
| 面板最小化 / White Bar 模式 | 待确认平台可行性 | v2 |
| 图片浏览模式 | 不实现 | v3 |
| 3D 模型格式支持 | 不实现（预留接口） | 未来版本 |
| 媒体库海报墙（类 Infuse） | 不实现 | 未来版本 |