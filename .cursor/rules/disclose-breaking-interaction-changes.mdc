---
description: 重大交互/能力替换前必须先告知用户影响面
alwaysApply: true
---

# 破坏性决策必须先披露

修改交互、呈现路径或系统能力时，若方案会**替换**既有系统控件/产品行为（例如把系统 `Menu` 换成自绘 `popover`，或换掉系统导航、反馈、命中模型），在动手前必须先明确告诉用户：

1. 打算改什么、用什么替换什么
2. 哪些现有实现可能因此变化或失效
3. 这是为实现哪个目标而做的权衡

未获用户确认前，不得以「为修好 A」为由静默改掉 B。隐蔽副作用比显式取舍更危险；宁可先停下来问，也不要造成用户事后才发现的功能破坏。

反例：为给 Menu 按钮做「打开保持按下」，静默改成自绘菜单，导致二级菜单面目全非、关闭闪烁、系统关闭音效丢失。

正例：先说明「系统 Menu 不暴露 isPresented，若要严格开/关按压态，可选：接受系统按压时序 / 或替换为自有呈现（会改变菜单外观与系统行为）。你要哪条？」再改。
